{
  "name": "souhailprompt",
  "version": "1.0.0",
  "description": "Souhail Prompt — Arabic-first cinematic video prompt generator. Runs fully in the browser.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.0"
  }
}
